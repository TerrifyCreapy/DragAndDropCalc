import {FC} from "react";

interface ISidebar {
    styles?: string;
    dragArea?: boolean
}

const SideBar:FC<ISidebar> = ({styles, dragArea}) => {
    return(
        <div>

        </div>
    );
}

export default SideBar;